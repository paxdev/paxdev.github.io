name: pax's dev blog
highlighter: rouge

defaults:
  -
    scope:
      path: ""      # empty string for all files
      type: posts   # limit to posts
    values:
      header: 
        backgroundImage: /assets/bg/earthrise.jpg
